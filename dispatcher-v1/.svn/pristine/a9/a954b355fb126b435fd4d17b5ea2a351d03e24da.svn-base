# !/usr/bin/python
# -*- coding: utf-8 -*-

# APP接口白名单，按照app_prefix + 接口方式写
def app_white_list(app_prefix):
    url_tuple = (
        app_prefix + '/network/common/net_job_resp',
        app_prefix + '/interface/status',
        app_prefix + '/catalog/vm/takeover',
    )
    return url_tuple