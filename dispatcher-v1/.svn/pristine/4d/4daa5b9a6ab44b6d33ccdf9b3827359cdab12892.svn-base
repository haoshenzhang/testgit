#!/usr/bin/python
# -*- coding: utf-8 -*-
"""
    dgl 2016-11-10
    work_service常量定义
"""

class ApiMapping(object):
    """
    dgl 服务流程节点与API的映射
    """
    def __init__(self):

        self.work_dic = dict(
            openstack_vm=u'http://service.travelsky.net/',
            alloc_ip = u'http://',
            bigeye_server=u'http://service.travelsky.net/'
        )                                   # 执行接口映射

        self.compute_dic = dict(

        )                                   # 计算接口映射
